# Full-Stack Project Root

This repository contains both the backend (Django) and mobile (Flutter) projects.

- `backend/`: Django backend
- `mobile/`: Flutter mobile app

## How to Use
- See each subfolder for setup instructions.
- Use the provided `.gitignore` files to avoid committing unnecessary files.
